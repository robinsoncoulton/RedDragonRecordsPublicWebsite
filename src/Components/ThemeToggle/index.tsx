import React from "react";
import { ThemeToggleProps } from "./types";

const ThemeToggle: React.FC<ThemeToggleProps> = ({ handleClick }) => {
  return <>theme toggler</>;
};

export default ThemeToggle;
